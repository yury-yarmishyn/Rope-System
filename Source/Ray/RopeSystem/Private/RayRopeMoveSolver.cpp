#include "RayRopeMoveSolver.h"

void FRayRopeMoveSolver::MoveSegment(
	const FRayRopeTraceSettings& TraceSettings,
	const FRayRopeMoveSettings& MoveSettings,
	FRayRopeSegment& Segment)
{
	static_cast<void>(TraceSettings);
	static_cast<void>(MoveSettings);
	static_cast<void>(Segment);

	// Placeholder for future segment movement or constraint solving logic.
}
